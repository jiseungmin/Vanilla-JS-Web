function math(){
  toString() // 10진수 -> 2진수 / 16진수
  let num = 10
  num.toString()
  num.toString(2)
  num.toString(16)

  Math.PI() // 3.141592653589793
  Math.ceil() // 올림
  Math.floor() // 내림
  toFixed(2) // 소숫점 자릿수 2자리
  
  Math.floor(Math.random()*100)+1 // 1~100까지 숫자를 뽑고 싶으면?
  Math.pow(n,m) // 제곱
  Math.sqrt(16) // 제곱근  // 4
}


